# PB4-C1 Master-Plan Addendum

Frozen state:

```text
PB3-B2  future bifurcation confirmed
PB4     control relevance not confirmed under four-action library
PB4-C0  alpha_formal = 0.25467727212168312
         nine-action normalized qpos family hard-limit feasible
```

Next allowed phase:

```text
PB4-C1 exploratory GPU audit
```

PB4-C1 must read the PB4-C0 family verbatim. It cannot recompute or modify
alpha, action IDs/order, H=20, 3 repeats, 0.05, 5x, or the 3-pair/2-fresh-live-
strata signal criterion.

Methodological invariant:

```text
classification = EXPLORATORY
formal_condition5_confirmed = false
```

Positive:

```text
PB4C1_NORMALIZED_QPOS_SIGNAL_FOUND
→ STOP
→ independent prospective confirmatory cohort required
```

Negative:

```text
PB4C1_NORMALIZED_QPOS_SIGNAL_NOT_FOUND
→ STOP Wrapping control-relevance route
→ no more alpha/horizon/action-family rescue
```

Sensing and all model training remain blocked until Condition 5 receives
confirmatory evidence or the paper claim is explicitly narrowed.
